---
task_family: test-api-k6-io
image: loadimpact/test-api.k6.io

cluster:
  production: demosites-cluster-production
  staging: demosites-cluster-staging

default_tags:
  production: latest
  staging: staging

task:
  command:
    - python
    - project/manage.py
    - migrate
    - --noinput
